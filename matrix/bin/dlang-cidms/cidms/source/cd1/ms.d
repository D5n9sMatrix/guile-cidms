module cd1.ms;

import std.algorithm;
import std.array;
import std.math;
import std.complex;
import std.container;
import std.digest;
import std.stdint;
import std.stdio;

version(GNU)
extern(D)
@system:

/********************************************************************************************************************
Although chemically unnamed, features are reliably tracked based on mass values and represent countable data points 
that can answer overarching questions. Do CD1 proteins capture few or many distinct lipids? Do the four CD1 protein 
types capture overlapping or distinct lipid profiles? Answering other questions required chemical naming, so collision-induced 
dissociation (CID)-MS (Figure S2) identified lipids in the second stage of analysis (Figure 2).
************************************************************************************************************************/
void aaLiteral(aaLiteral, abs, acos, acosh)(ref among)
{
    aaLiteral Details;
    abs CIDMSProtein;
    acos CD1Protein;

    void DList (DList, Digest, DigestType)(ref digest)
    {
        DList.back                 = norm;
        DList.clear                = 0;
        DList.empty                = " ";
        DList.front                = endsWith;
        DList.insertAfter          = partialSort;
        DList.insertBack           = partialSort;
        DList.insertBefore         = partialSort;
        DList.insertFront          = partialSort;
        DList.linearRemove         = 0;
        DList.linearRemoveElement  = 512;
        DList.opBinary             = 11;
        DList.opBinaryRight        = 12;
        DList.opEquals             = 1;
        DList.opOpAssign           = readf;
        DList.opSlice              = readln;
        DList.popFirstOf           = poly;
        DList.stuff                = 512;
        DList.values               = 2325;
        DList.Range                = 0;
        DList.T                    = 0;
    }            
Digest Obj;
}    
    

